# Runtime deck tile state (hooks, hand-written tile_*.json)
scripts/deck_state/

# Node deps (run `npm install` in the plugin folder on deploy)
node_modules/

# codedb tool snapshot
codedb.snapshot

# macOS
.DS_Store
